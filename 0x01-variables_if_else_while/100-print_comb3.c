#include<stdio.h>
/**
 * main -all the excution carried out here
 * Return: returns 0.
 */
int main(void)
{

	int count,numCount, alphaCount;

	count = 1;
	numCount = 57;
	alphaCount = 48;
	while (alphaCount <=numCount)
	{
		while (count<10))
		{
		putchar(alphaCount);
		putchar(numCount);
		count++
		numCount++;
		}
	putchar(alphaCount);
	alphaCount++;
	}
	putchar(10);
	return (0);
}
